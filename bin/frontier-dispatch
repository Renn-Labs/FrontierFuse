#!/usr/bin/env bash
# FrontierFuse orchestrator body-caller shim -> frontier_dispatch.py
set -euo pipefail
DIR="$(python3 -c 'import os, sys; print(os.path.dirname(os.path.dirname(os.path.realpath(sys.argv[1]))))' "$0")"
exec python3 "$DIR/frontier_dispatch.py" "$@"
